/**
 * Placeholder platform identity. "Silk Road" is intentionally not
 * rendered anywhere in the UI right now — every component that used to
 * hardcode it reads from here instead, so swapping to the final name
 * later is a one-line change in this file, not a find-and-replace
 * across components.
 */
export const PLATFORM_NAME = "Nightfall";
export const PLATFORM_TAGLINE = "Cyber Intelligence Division";